const chose_action = require('./chose_action_block')
const score_blocks = require('./score_block')
const connect_blocks = require('./connect_block')

module.exports = {
    chose_action,
    score_blocks,
    connect_blocks
}